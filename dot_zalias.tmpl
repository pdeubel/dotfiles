{{- if or (eq .chezmoi.osRelease.id "arch") (eq .chezmoi.osRelease.id "manjaro") -}}
alias ll="exa -alh"
{{- end }}

alias eI="chezmoi edit ~/.config/i3/config"

# Kubernetes
alias k="kubectl"
alias h="helm"
